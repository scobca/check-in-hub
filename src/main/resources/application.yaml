server:
  port: ${SERVER_PORT}

spring:
  application:
    name: check-in-hub

  r2dbc:
    url: r2dbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
    username: ${DB_USER}
    password: ${DB_PASSWORD}

database:
  host: ${DB_HOST}
  port: ${DB_PORT}
  name: ${DB_NAME}
  username: ${DB_USER}
  password: ${DB_PASSWORD}
  schema: ${DB_SCHEMA}

config:
  cors:
    frontend_local_host: ${FRONTEND_LOCAL_HOST}
    frontend_docker_host: ${FRONTEND_DOCKER_HOST}
    frontend_production_host: ${FRONTEND_PRODUCTION_HOST}